package com.IFPI.CLINICA.repository;

import com.IFPI.CLINICA.model.EnderecoPaciente;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EnderecoPacienteRepository extends JpaRepository<EnderecoPaciente, Integer>{
}
