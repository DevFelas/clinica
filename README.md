## ▶️ Como executar a aplicação

Para iniciar a aplicação, siga os passos abaixo:

1. Acesse o diretório do projeto, onde está localizado o arquivo `pom.xml`:
   
   ```bash
   cd CLINICA
2. Execute a aplicação utilizando o Maven:

    ```bash
    mvn javafx:run
